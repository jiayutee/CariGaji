# Single source of truth for orchestrator headless allowed tools.
# One tool per line. Lines starting with # and blank lines are ignored.
# The runner joins these with commas into --allowedTools.
#
# NOTE: The claude.ai Notion MCP connector is NOT available in headless
# (launchd) runs — it only exists inside an interactive Claude Code session.
# The orchestrator therefore uses the Notion REST API via curl (Bash tool)
# with NOTION_TOKEN from .env. No Notion MCP entries here on purpose.
Bash
Read
Edit
Write
Agent
WebFetch
WebSearch
